function myName (nameOne, nameTwo) {
	var combinedNames = joinedStrings(nameOne, nameTwo);
	console.log(combinedNames);
}


function joinedStrings(string1, string2) {
	return string1 + " & " + string2;
}

function myTwoFavoriteSongs(song1, song2) {
	var joinedSongs = joinedStrings(song1, song2);
	console.log(joinedSongs);
}